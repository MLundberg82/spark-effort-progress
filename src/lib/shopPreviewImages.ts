import { RatTier } from "./gamificationStore";

export function getItemPreviewImage(_itemId: string, _tier: RatTier): string | null {
  return null;
}

export function hasItemPreview(_itemId: string, _tier: RatTier): boolean {
  return false;
}